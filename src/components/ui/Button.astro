---
import { cn } from '../../lib/cn';

interface Props {
  href?: string;
  class?: string;
  variant?: 'primary' | 'secondary' | 'ghost';
  type?: 'button' | 'submit';
}

const { href, class: className, variant = 'primary', type = 'button' } = Astro.props;

const variants = {
  primary: 'bg-brand-accent text-white hover:bg-brand-accent/90',
  secondary: 'bg-transparent text-gray-100 border border-brand-border hover:bg-white/5 hover:border-brand-border-active',
  ghost: 'text-gray-300 hover:text-white',
};

const classes = cn(
  'inline-flex h-11 items-center justify-center gap-2 rounded-lg px-5 text-sm font-semibold transition-all duration-200',
  variants[variant],
  className
);
---

{href ? (
  <a href={href} class={classes}><slot /></a>
) : (
  <button type={type} class={classes}><slot /></button>
)}
